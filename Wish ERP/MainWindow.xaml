<Window x:Class="Wish_ERP.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="621" Width="902" MinWidth="900" MinHeight="600">

    <Border Padding="5">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions>

            <!-- Navigator -->
            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Text="DreamWish" TextAlignment="Center" FontSize="20" Margin="20"/>
                <StackPanel Grid.Row="2">
                    <Button Content="Clients" Margin="1" Click="OpenClientView"/>
                    <Button Content="Products" Margin="1" Click="OpenProductsView"/>
                    <Button Content="Orders" Margin="1" Click="OpenOrdersView"/>
                    <TextBlock x:Name="textblock" Height="392"/>
                </StackPanel>
                
            </Grid>

            <!-- Content Component -->
            <Grid Grid.Column="1">
                <ContentControl x:Name="CurrentPage"/>
            </Grid>
        </Grid>
    </Border>
</Window>
