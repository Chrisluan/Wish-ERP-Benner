<Window x:Class="Wish_ERP.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="621" Width="902" MinWidth="900" MinHeight="600">

    <Border Padding="5">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions>

            <!-- Navigator -->
            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Text="DreamWish" TextAlignment="Center" FontSize="20" Margin="5"/>
                <Button Grid.Row="1" Content="Clients" Margin="5" Click="OpenClientView"/>
                <Button Grid.Row="2" Content="Products" Margin="5" Click="OpenProductsView"/>
                <Button Grid.Row="3" Content="Orders" Margin="5" Click="OpenOrdersView"/>
            </Grid>

            <!-- Content Component -->
            <Grid Grid.Column="1">
                <ContentControl x:Name="CurrentPage"/>
            </Grid>
        </Grid>
    </Border>
</Window>
