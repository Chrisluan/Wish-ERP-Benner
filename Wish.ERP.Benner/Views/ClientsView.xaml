<UserControl x:Class="Wish_ERP.Views.ClientsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Wish_ERP.Views"
             xmlns:view="clr-namespace:Wish_ERP.Views.Components"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.2*" MaxWidth="400"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <DockPanel LastChildFill="True">
                <Button Padding="5" Margin="10,0,0,0" Content="Pesquisar" Click="AddClient" DockPanel.Dock="Right"/>
                <TextBox Padding="5" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>
            </DockPanel>

            <DockPanel Grid.Column="1" LastChildFill="True" HorizontalAlignment="Right">
                <Button Padding="5" IsEnabled="False" Click="DeleteClient" x:Name="DeleteClientButton" Content="Deletar"  Margin="10,0,0,0"/>

                <Button Padding="5" IsEnabled="True" Click="CreateClient" Margin="10,0,0,0" Content="Novo" DockPanel.Dock="Right"/>
                <Button Padding="5" IsEnabled="False" x:Name="EditClientButton" Click="EditClient" Margin="10,0,0,0" Content="Editar" DockPanel.Dock="Right"/>

            </DockPanel>

        </Grid>

        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            
            <ListView Margin="0,5,0,0" SelectionMode="Extended" SelectionChanged="HandleSelectionList" Background="Transparent" Grid.Row="0" Name="ClientsList" ItemsSource="{Binding FilteredClientList}">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="ID" DisplayMemberBinding="{Binding Id}" Width="50"/>
                        <GridViewColumn Header="CPF" DisplayMemberBinding="{Binding CPF}" Width="200"/>
                        <GridViewColumn Header="Nome" DisplayMemberBinding="{Binding Name}" Width="200"/>
                        <GridViewColumn Header="Endereço" DisplayMemberBinding="{Binding Address}" Width="200"/>
                    </GridView>
                </ListView.View>
            </ListView>

            <ContentControl Grid.Row="1" x:Name="SelectedClientViewer"></ContentControl>
            
        </Grid>
    </Grid>

</UserControl>
