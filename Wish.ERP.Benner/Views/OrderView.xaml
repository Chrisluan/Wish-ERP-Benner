<UserControl x:Class="Wish_ERP.Views.OrderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Wish_ERP.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" MaxWidth="400"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <DockPanel LastChildFill="True">
                
                
                <TextBox  Padding="5" Width="200" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Margin="10,0">Valor Entre</TextBlock>
                    <TextBox PreviewTextInput="TextBox_PreviewTextInput" Text="{Binding FilterMinValue, UpdateSourceTrigger=PropertyChanged}" Width="40"></TextBox>
                    <TextBlock Margin="10,0">E</TextBlock>
                    <TextBox PreviewTextInput="TextBox_PreviewTextInput" Text="{Binding FilterMaxValue, UpdateSourceTrigger=PropertyChanged}" Width="40"></TextBox>
                </StackPanel>
            </DockPanel>

            <DockPanel Grid.Column="1" HorizontalAlignment="Right">
                <Button Padding="5" IsEnabled="False" Click="DeleteClient" x:Name="DeleteClientButton" Content="Deletar"  Margin="10,0,0,0"/>
                <Button Padding="5" IsEnabled="True" Click="CreateOrder" Margin="10,0,0,0" Content="Novo" DockPanel.Dock="Right"/>
                <Button Padding="5" IsEnabled="False" x:Name="EditClientButton" Click="EditClient" Margin="10,0,0,0" Content="Editar" DockPanel.Dock="Right"/>
            </DockPanel>

        </Grid>


        <ListView Margin="0,5,0,0"
          SelectionMode="Extended"
          SelectionChanged="HandleSelectionList"
          Background="Transparent"
          Grid.Row="1"
          Name="OrdersList"
          ItemsSource="{Binding FilteredOrders}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="ID" Width="50">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Id}"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>

                    <GridViewColumn Header="Data da Venda" Width="200">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding SaleDate}"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>

                    <GridViewColumn Header="Produtos" Width="200">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding ProductsNames}"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>

                    <GridViewColumn Header="Preço Total" Width="200">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding TotalOrderPrice}"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>

                    <GridViewColumn Header="Quantidade" Width="200">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding OrderBoxes}"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>

        
                    <GridViewColumn Header="Ações" Width="160">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    
                                    
                                    <Button Content="Excluir"
                                    Margin="2"
                                    Padding="5,2"/>
                                </StackPanel>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>

    </Grid>

</UserControl>
