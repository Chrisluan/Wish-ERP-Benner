<Window x:Class="Wish_ERP.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="621" Width="902" MinWidth="900" MinHeight="600">

    <Border Padding="10">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions>

            <!-- Navigator -->
            <Grid Grid.Column="0" Background="#E6E7E8">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Text="DreamWish" TextAlignment="Center" FontSize="20" Margin="20"/>
                <StackPanel Grid.Row="2" Margin="0,0,5,0">
                    <Button Content="Clients" Margin="1" Click="OpenClientView"/>
                    <Button Content="Products" Margin="1" Click="OpenProductsView"/>
                    <Button Content="Orders" Margin="1" Click="OpenOrdersView"/>
                   
                </StackPanel>
                
            </Grid>

            
            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <!-- Current Page Component -->
                <Grid Grid.Row="0" Background="#D2D3D5" >
                    <TextBlock HorizontalAlignment="Left" Grid.Column="0" x:Name="ActionBar" Text="DreamWish ERP" FontSize="24" FontWeight="Bold" Margin="10"/>
                </Grid>
                
                <!-- Content Component -->
                <Border Padding="5" Background="#BDBFC1" CornerRadius="5" Margin="5" Grid.Row="1">
                    <ContentControl x:Name="CurrentPage"/>
                </Border>

            </Grid>
            
        </Grid>
    </Border>
</Window>
